git remote show server
-> fetch랑, push 대상 확인
git remote -v
-> 모든 remote 확인

====================
SSH Key
(계정에서 SSH key를 설정할 수 있음)
->github에서 제공하는 문서를 따라하면된다.

secure shell protocol

우리가 사용하는 terminal과 server간에 안전하게
아이디와 비밀번호를 유지해주는 것임

server에 public key를

나의 컴퓨터에는 private key를 생성함

